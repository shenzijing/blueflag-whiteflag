# Cloudflare Pages 响应头配置
# 此文件需要放在游戏根目录（与index.html同级）

/*
  # 允许被 bluewhiteflag.com 嵌入iframe
  Content-Security-Policy: frame-ancestors 'self' https://bluewhiteflag.com https://*.bluewhiteflag.com
  X-Frame-Options: ALLOW-FROM https://bluewhiteflag.com
  
  # CORS配置（如果需要）
  Access-Control-Allow-Origin: https://bluewhiteflag.com
  Access-Control-Allow-Methods: GET, HEAD, OPTIONS
  Access-Control-Allow-Headers: *
  
  # 安全头
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  
  # 缓存控制
  Cache-Control: public, max-age=31536000, immutable

# 对于HTML文件，不要缓存太久
/*.html
  Cache-Control: public, max-age=3600, must-revalidate

